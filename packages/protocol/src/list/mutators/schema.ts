/**
 * The mutation envelope schema lives in `./index.ts` as a discriminated
 * union assembled from per-mutator argsSchemas. Wire-level envelope
 * metadata (`accountId`, `timestamp_client`) lives in `./_shared.ts`.
 * This file is intentionally empty.
 */
export {};
